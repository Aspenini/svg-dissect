{
  "name": "svg-disect",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun ./index.html",
    "build": "bun build ./index.html --outdir=dist --minify",
    "preview": "bun ./dist/index.html"
  }
}
